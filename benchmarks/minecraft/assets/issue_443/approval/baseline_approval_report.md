# Minecraft baseline approval

Status: APPROVED

The archives and restored trees are distinct, while initial state and canonical targets match. `central_wall_v1` changes 13 blocks.

Position convention: `entity_feet`.

The unchanged approval evidence reaches all three canonical targets on both baselines (6/6), with strict Y delta `0.000`. The lower floor is full-block terrain at block Y `-61`, so the diagonal and long-distance entity-feet targets at Y `-60` are valid standing positions. The raised platform is full-block terrain at block Y `-60`, so the initial and near entity-feet positions at Y `-59` are valid.

The convention attestation does not replace or rewrite either historical archive or acquisition provenance.
